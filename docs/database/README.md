# Database

Planned for a later sprint — see [ROADMAP.md](../../ROADMAP.md).

This directory will hold PostgreSQL schema docs and Qdrant collection schemas. Initial DB init scripts live in [scripts/init-db](../../scripts/init-db).
